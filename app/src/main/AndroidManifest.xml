<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.nirjhor.offlinecalling">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity" />
        <activity android:name=".MakeCall" />
        <activity android:name=".MessageSend" />
        <activity android:name=".GroupMessage" />
        <activity android:name=".GroupCall" />
        <activity android:name=".DbTest"/>
        <activity android:name=".CarListActivity" />
        <activity android:name=".MessageListActivity" />
        <activity android:name=".Db_Test2">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>